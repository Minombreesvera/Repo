#include "main.h"
/**
 * print_alphabet - To print the alphabets in lowercase
 * Return: 0
 */
void print_alphabet(void)
{
	char vera;

		for (vera = 'a'; vera <= 'z'; vera++)
		{
		_putchar(vera);
		_putchar('\n');
		}
		return (0);
}
